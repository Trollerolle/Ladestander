<Page x:Class="El_Booking.View.Booking.YourBookingPage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:local="clr-namespace:El_Booking.View.Booking"
	  mc:Ignorable="d"
	  d:DesignHeight="450"
	  d:DesignWidth="800"
	  Title="YourBooking">

	<Page.Resources>
		<Style x:Key="TextBlockStyler"
			   TargetType="TextBlock">
			<Setter Property="FontWeight"
					Value="Bold" />
			<Setter Property="VerticalAlignment"
					Value="Center"/>
			<Setter Property="DockPanel.Dock"
					Value="Left" />
		</Style>

		<Style x:Key="WrapPanelStyler" TargetType="WrapPanel">
			<Setter Property="Width"
					Value="200" />
			<Setter Property="VerticalAlignment"
					Value="Center" />
			<Setter Property="HorizontalAlignment"
					Value="Center" />
			<Setter Property="Margin"
					Value="20,0,0,20" />
			<Setter Property="ItemWidth"
					Value="80" />
		</Style>

		<Style x:Key="DockPanelStyler"
			   TargetType="DockPanel">
			<Setter Property="Width"
					Value="165" />
			<Setter Property="VerticalAlignment"
					Value="Center" />
			<Setter Property="DockPanel.Dock"
					Value="Left" />
			<Setter Property="Margin"
					Value="0,0,0,20" />
		</Style>

	</Page.Resources>
	<Grid Background="White">
		<Grid Margin="215,30,215,30">
			<Grid.RowDefinitions>
				<RowDefinition Height="20*" />
				<RowDefinition Height="25*" />
				<RowDefinition Height="55*" />
			</Grid.RowDefinitions>
			<Border Grid.Row="0"
					Background="{DynamicResource Background1}"
					CornerRadius="8,8,0,0">
				<TextBlock Grid.Row="0"
						   Text="Din Booking"
						   FontSize="25"
						   HorizontalAlignment="Center"
						   VerticalAlignment="Center" />
			</Border>
			<Border Grid.Row="1"
					Margin="0"
					Background="{DynamicResource Background2}">
				<DockPanel Style="{StaticResource DockPanelStyler}">
					<TextBlock Text="Dato: "
							   Style="{StaticResource TextBlockStyler}"/>
					<Label Content="{Binding CurrentBooking.Date, FallbackValue=Ingen aktuel booking.}"
						   ContentStringFormat="dd/MM/yyyy" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
				</DockPanel>
			</Border>
			<Border Grid.Row="2"
					Margin="0"
					Background="{DynamicResource Background1}"
					CornerRadius="0,0,8,8">
				<StackPanel Margin="20"
							VerticalAlignment="Center"
							HorizontalAlignment="Center">
					<WrapPanel Style="{StaticResource WrapPanelStyler}">
						<TextBlock Text="Start tid: "
								   Style="{StaticResource TextBlockStyler}"/>
						<Label Content="{Binding CurrentBooking.TimeSlot.TimeSlotStart}"
							   ContentStringFormat="HH:mm"
							   HorizontalAlignment="Right" />
					</WrapPanel>
					<WrapPanel Style="{StaticResource WrapPanelStyler}">
						<TextBlock Text="Slut tid: "
								   Style="{StaticResource TextBlockStyler}"/>
						<Label Content="{Binding CurrentBooking.TimeSlot.TimeSlotEnd}"
							   ContentStringFormat="HH:mm"
							   HorizontalAlignment="Right" />
					</WrapPanel>
					<WrapPanel Style="{StaticResource WrapPanelStyler}">
						<TextBlock Text="Ladestander nr.: "
								   Style="{StaticResource TextBlockStyler}"/>
						<Label Content="{Binding CurrentBooking.ChargingPointID}"
							   HorizontalAlignment="Right" />
					</WrapPanel>
					<Button Margin="0,10,0,10"
							Width="135.55666666666667"
							Content="Slet Booking"
							Command="{Binding DeleteBookingCommand}" />
				</StackPanel>

			</Border>
		</Grid>
	</Grid>
</Page>
